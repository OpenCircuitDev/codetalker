[project]
name = "claude-code-talker"
version = "0.1.0"
description = "Claude Code Talker — multi-mode voice companion for Claude Code"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "OpenCircuitDev" }]
