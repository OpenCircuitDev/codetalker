[project]
name = "codetalker"
version = "0.1.0"
description = "Multi-mode voice companion for Claude Code"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "OpenCircuitDev" }]
